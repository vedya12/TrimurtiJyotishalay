// import { isLoggedIn } from '../lib/auth.js';

// if (!isLoggedIn()) {
//   window.location.href = '/login.html?redirect=' + encodeURIComponent(window.location.pathname);
// }

// Bypassed authentication check for demo / local testing mode
console.log('Demo Mode: Navigation guard active (auth check bypassed)');